<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Edit Floral Shops</title>
    <link rel="stylesheet" href="/stylesheets/style.css" />
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Spicy+Rice&display=swap" rel="stylesheet">
  </head>
  <body class="shop-page">
    <header class="page-header">
      <h1>Edit <%= shop.name %></h1>
    </header>
    <form class="shop-form" action="/shops/<%= shop._id %>?_method=PUT" method="POST">
      <div class="form-group">
        <label for="name">Name:</label>
        <input type="text" name="name" value="<%= shop.name %>" id="name" />
      </div>
  
      <div class="form-group">
        <label for="offersDelivery">Offers Delivery</label>
        <input type="checkbox" name="offersDelivery" <% if (shop.offersDelivery) { %>checked<% } %> id="offersDelivery">
      </div>

      <div class="form-group">
        <button type="submit" class="submit-button">Update Shop</button>
      </div>
    </form>
    <footer class="footer">
      <a href="/shops" class="back-link">Back to Shops</a>
    </footer>
  </body>
</html>